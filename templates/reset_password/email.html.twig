<h1>Bonjours</h1>

<p>Pour reinitialiser votre mot de passe, cliquez sur le liens suivant :</p>

<!--<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>-->

<a href="https://127.0.0.1:8000/reset-password/reset/{{ resetToken.token }}">Restaurer son mot de passe</a>

<p>Ce liens expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cordialement</p>
